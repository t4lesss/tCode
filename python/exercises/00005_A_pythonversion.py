"""
github.com/t4lesss 

Print the Python version to the console.
#Expected result: 3.8.10

"""

import sys;

p = print;

p(sys.version.split()[0]);
# Retorna a primeiro ínidice da lista criada pela função split, que é a versão do Python.
# Returns first index of the list created by split() function, which is the Python version.